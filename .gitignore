# ==============================================================================
#  柿果成熟度识别 · 蔬菜水果分类 —— Git 忽略规则
#  原则：忽略"能自动重建的"和"换台电脑就失效的"，保留"别人拿到就能跑的"。
# ==============================================================================

# ---------- Python ----------
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/

# ---------- 虚拟环境 ----------
.venv/
venv/
env/
ENV/

# ---------- IDE ----------
.idea/
.vscode/
*.swp
*.swo

# ---------- 缓存与配置（自动重建 / 换机失效）----------
.ultralytics/
Ultralytics/
.mplcache/
dataset/*.cache
dataset_persimmon/*.cache

# ---------- 大数据集：不进仓库，用脚本重建 ----------
# dataset_persimmon/ 约 191 MB（平均 888 KB/张，手机截图 + 实拍原图），
# 提交后 clone 会很慢，所以排除。重建方式：
#     python build_dataset_persimmon.py --source "你的分类图片目录"
dataset_persimmon/
images/

# ---------- 训练输出里的噪音 ----------
runs/**/train_batch*.jpg
runs/**/val_batch*.jpg
runs/**/weights/
runs/classify/
runs/predict*/
runs/camera_snapshots/
runs/camera_test.jpg

# ---------- 本地临时文件 ----------
_tmp_*
*.bak

# ---------- 操作系统 ----------
Thumbs.db
Desktop.ini
.DS_Store

# ---------- 杂项 ----------
*.log
*.tmp
~$*

# ==============================================================================
#  以下内容是【故意上传】的，不作忽略：
#    dataset/                    物种数据集 28 MB（12 类，别人 clone 下来要能直接训练）
#    weights/yolo11n-cls.pt      预训练权重 5.5 MB（github.com 域名解析被污染，
#                                放进仓库省得别人下不来）
#    models/fruits_cls_v1/       物种模型 best.pt（97.95%）
#    models/fruits_cls_v2/       柿果成熟度模型 best.pt（84.44%）
#    runs/fruits_cls_v1/         物种模型训练曲线、混淆矩阵、results.csv、args.yaml
#    runs/fruits_cls_v2/         柿子模型训练曲线、混淆矩阵、results.csv、args.yaml
#    runs/fruits_cls_v1_eval/    中文标签混淆矩阵
#    *.py / README.md            代码本体
#
#  以下内容【故意不上传】，需用脚本重建：
#    dataset_persimmon/          柿子数据集 191 MB
#                                → python build_dataset_persimmon.py
# ==============================================================================
